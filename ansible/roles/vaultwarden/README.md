variables:
```yaml
# optional
vaultwarden_admin_pass: # if undefined will disable admin panel
vaultwarden_url: "https://vault.{{ domain }}"
vaultwarden_data_dir: /vaultwarden-data
vaultwarden_force_reload: false
vaultwarden_port: 8081
```
