remember when setting other services or proxying sites to always restrict access by ip if needed

variables:
```yaml
# optional
nginx_external: false # if proxy accessible from outside
wireguard_iface: # if different than wg0
```
